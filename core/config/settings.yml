# Paths are not listed here: the tooling locates itself from the checkout, for
# whoever is running it. Add root/path/scripts/config/backups only to override
# that (or export WEBSHIP_WORKSPACE_ROOT/PATH/SCRIPTS/CONFIG).
host: 127.0.0.1
web: http://127.0.0.1/core
protocol: http
account:
  name: webmaster
  pass: CHANGE_ME
  mail: info@webship.co
config_sync_directory: ../config/sync
automated_testing:
  wd_host: 127.0.0.1:4444/wd/hub
openai:
  api_key: sk-change-this
  api_org: org-change-this
workspaces:
  - products
  - dev
  - test
  - demos
  - sandboxes
  - profiles
  - modules
  - themes
  - libraries
  - forked
  - recipes
  - components
  - agents
  - skills
  - prompts
  - docs
  - worklogs
