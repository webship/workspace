# Site templates — workspace context

You are inside the **site-templates** workspace of the Webship Workspace (`~/workspace`).

@AGENTS.md

The rules in it apply to everything under this folder, however deep.
