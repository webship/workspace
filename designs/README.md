# Designs

Design work for the things this workspace builds: the running design-tool instances, and the
sources and exports beside them.

An instance is a DDEV project like any other, so it is built, started, stopped and backed up from
the dashboard page the same way. The exports live in the same folder because a mockup is only
useful next to the thing it describes.
