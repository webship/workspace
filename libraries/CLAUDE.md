# Libraries — workspace context

You are inside the **libraries** workspace of the Webship Workspace (`~/workspace`).

@AGENTS.md

The rules in it apply to everything under this folder, however deep.
